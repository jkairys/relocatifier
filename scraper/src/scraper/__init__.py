"""Relocatifier listings-acquisition service.

Single-flight OTH `recentlysold` scrape into a per-operation DuckDB store
(ADR-0006), exporting a sales artifact for the static frontend (ADR-0007).
"""
